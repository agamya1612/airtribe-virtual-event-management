# 🎯 Virtual Event Management Platform (In-Memory Backend)

A simple **Node.js + Express** backend for managing virtual events with **user authentication**, **event scheduling**, and **participant management** — all stored in memory (no database required).

This app supports:
- ✅ User registration & login (with bcrypt password hashing)
- ✅ JWT-based authentication
- ✅ Role-based access (Organizers vs Attendees)
- ✅ Create, update, delete events (organizers only)
- ✅ Attendee event registration
- ✅ HTML forms for Register, Login, and Event Dashboard
- ✅ In-memory data structures for fast prototyping

---

## 📦 Requirements
- [Node.js](https://nodejs.org/) (v14+ recommended)
- npm (comes with Node.js)

---

## 🚀 Setup & Run

1. **Clone or download** the repository  
   ```bash
   git clone https://github.com/your-username/virtual-event-platform.git
   cd virtual-event-platform
