// data/memoryStore.js
let users = []; // { id, name, email, passwordHash, role }
let events = []; // { id, title, date, time, description, organizerId, participants: [] }

module.exports = { users, events };
